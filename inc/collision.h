#ifndef COLLISION_H
#define COLLISION_H

// Declaration for collision detection
bool checkCollision(float newX, float newY);
void handleCollision(float& playerX, float& playerY, float newX, float newY);

#endif
